(define (problem tidy-task2)
  (:domain task2)

  (:objects
    ; PLACEHOLDER: item declarations
    ; PLACEHOLDER: location declarations
    ; PLACEHOLDER: room declarations
  )

  (:init
    (free_gripper)
    (agent_at START_LOCATION)

    ; PLACEHOLDER: untidy predicates
    ; PLACEHOLDER: tidy predicates
    ; PLACEHOLDER: tidying predicates

    ; PLACEHOLDER: scan_loc predicates
    ; PLACEHOLDER: location_in_room predicates
    ; PLACEHOLDER: adjacent predicates
    ; PLACEHOLDER: item_at predicates
  )

  (:goal (and
    ; All rooms must be visited and marked tidy and all objects must be placed to the correct drop location

    ; PLACEHOLDER: on_drop_loc goals
    ; PLACEHOLDER: tidy goals
  ))

)


